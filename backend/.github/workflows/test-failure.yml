name: Test Failure

on:
  push:
    branches: [main]

jobs:
  fail:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Fail intentionally
        run: exit 1
